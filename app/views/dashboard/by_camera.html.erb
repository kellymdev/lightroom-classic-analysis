<h1>Analysis by Camera</h1>

<h2><%= @data_by_camera[:camera] %></h2>
<dl>
  <dt>Keywords:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:keywords].each do |keyword| %>
        <li><%= keyword %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Focal Lengths:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:focal_lengths].each do |focal_length| %>
        <li><%= focal_length %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Shutter Speeds:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:shutter_speeds].each do |shutter_speed| %>
        <li><%= shutter_speed %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Isos:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:isos].each do |iso| %>
        <li><%= iso %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Months:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:months].each do |month| %>
        <li><%= month %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Years:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:years].each do |year| %>
        <li><%= year %></li>
      <% end %>
    </ul>
  </dd>

  <dt>Month Year Combinations:</dt>
  <dd>
    <ul>
      <% @data_by_camera[:month_year_combinations].each do |month_year| %>
        <li><%= month_year %></li>
      <% end %>
    </ul>
  </dd>
</dl>