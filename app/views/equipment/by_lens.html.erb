<h1>Analysis by Lens</h1>

<h2><%= @data_by_lens[:lens] %> (<%= @data_by_lens[:years_covered] %>)</h2>

<%= render partial: 'image_count', locals: { image_count: @data_by_lens[:image_count] } %>

<dl>
  <%= render partial: 'shared/data_list', locals: { heading: 'Keywords', data: @data_by_lens[:keywords] } %>

  <%= render partial: 'shared/data_list', locals: { heading: 'Cameras', data: @data_by_lens[:cameras] } %>

  <%= render partial: 'shared/data_list_with_prefix_and_suffix', locals: { heading: 'Focal Lengths', data: @data_by_lens[:focal_lengths], item_prefix: nil, item_suffix: 'mm' } %>

  <%= render partial: 'shared/data_list_with_prefix_and_suffix', locals: { heading: 'Shutter Speeds', data: @data_by_lens[:shutter_speeds], item_prefix: nil, item_suffix: 's' } %>

  <%= render partial: 'shared/data_list', locals: { heading: 'Isos', data: @data_by_lens[:isos] } %>

  <%= render partial: 'shared/ratings_by_equipment', locals: { data: @data_by_lens[:ratings] } %>

  <%= render partial: 'shared/data_list', locals: { heading: 'Months', data: @data_by_lens[:months] } %>

  <%= render partial: 'shared/data_list', locals: { heading: 'Years', data: @data_by_lens[:years] } %>

  <%= render partial: 'shared/data_list', locals: { heading: 'Month Year Combinations', data: @data_by_lens[:month_year_combinations] } %>
</dl>